/** Static identity for this build — see PROTOTYPES.md at repo root. */
export const PROTOTYPE_DEPLOYMENT_META = {
  channelLabel: "Lab prototype",
  purposeLine:
    "Internal spikes and layout experiments — not for stakeholder milestone demos.",
} as const;
